# Agentic Demo — Project Rules

## Tech Stack
- **Language:** Python 3.11
- **Framework:** Flask 3.x
- **Testing:** pytest
- **Linting:** ruff
- **Type checking:** (none for this demo)

## Build & Test Commands
- Install deps: `pip install -r requirements.txt`
- Run tests: `pytest tests/ -v`
- Run single test: `pytest tests/test_store.py::test_name -v`
- Lint: `ruff check src/ tests/`
- Auto-fix lint: `ruff check --fix src/ tests/`

## Code Style
- Type hints on all public functions
- Google-style docstrings
- No wildcard imports
- Use `from __future__ import annotations` for modern type syntax

## Git Workflow
- Branch naming: `feat/`, `fix/`, `refactor/`, `docs/`
- Commit format: Conventional Commits (`feat:`, `fix:`, `chore:`)
- Always run `ruff check` and `pytest` before commit

## Architecture
- `src/store.py` — in-memory URL store (core logic, no framework deps)
- `src/app.py` — Flask API layer (thin, delegates to store)
- `tests/` — mirrors src/ structure

## TDD Enforcement
- Write failing test FIRST (RED)
- Minimal implementation (GREEN)
- Clean up (REFACTOR)
- Every new function MUST have a test before implementation
